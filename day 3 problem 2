#include <studio.h>

void update(int *a,int*b)
{

   ints=*a;
   *a=*a+*b;
   if (s>*b)
     { *b=s*b;
       }
else
     {*b= *b-s;
     }
}
int main () {
int a,b;
int *pa= &a,  *pb= &b;

scanf ("%d % d", &a, &b);
update (pa,pb);
printf("%d\n%d",a,b);
 return 0;
 }
       
